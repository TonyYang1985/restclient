mo:
  userservice:
    test3:
      method: "get" # default value is get if remove method define.
      endPoint: "{{baseURL}}?format={{format}}"
    test4:
      method: "get" # default value is get if remove method define.
      endPoint: "{{baseURL}}?format={{format}}"
